export default function Footer() {
  return (
    <footer className="border-t border-hairline mt-16 py-8 text-center text-sm text-ink-soft">
      RecipeHub — built for sharing what's actually in the pot.
    </footer>
  );
}
